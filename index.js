/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PageNavigation from './src/views/navigation/PageNavigation';
import {name as tutorial1} from './app.json';

AppRegistry.registerComponent(tutorial1, () => PageNavigation);
